FROM jenkins/jenkins:lts

# Jenkins Server Configuration
# Container: jenkins_server
# Volume: jenkins_data (mounted at /var/jenkins_home)
# Port: 8080 (HTTP), 50000 (Agent communication)

EXPOSE 8080 50000

# Add custom plugins or configuration here if needed
# RUN jenkins-plugin-cli --plugins <plugin-names>
